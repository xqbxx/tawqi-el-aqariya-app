using Microsoft.AspNetCore.SignalR;

namespace TawqiApi.Hubs
{
    public class PropertyHub : Hub
    {
        // Clients will listen to events sent from the server.
        // We don't necessarily need methods here unless clients send messages to the server via Hub.
    }
}
